<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.list">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".l1" />
        <activity android:name=".l2" />
        <activity android:name=".l3" />
        <activity android:name=".list2" />
        <activity android:name=".ListSearch" />
        <activity android:name=".listsearch2" />
        <activity android:name=".ListSearch3" />
        <activity android:name=".ActivityLife" />
        <activity android:name=".CameraApp" />
        <activity android:name=".SurfaceViewExample"></activity>
    </application>

</manifest>